create materialized view if not exists mv62 as select cast_info.movie_id AS movie_id, cast_info.person_id AS person_id, info_type.id AS info_type_id_2, info_type.info AS info_type_info_3, link_type.id AS link_type_id_4, link_type.link AS link, name.gender AS gender, name.name AS name, name.name_pcode_cf AS name_pcode_cf, person_info.info AS person_info_info_9, person_info.note AS note
 from link_type,cast_info,info_type,name,person_info,movie_link
 where (movie_link.link_type_id = link_type.id) And (link_type.link in ('references', 'referenced in', 'features', 'featured in')) And (cast_info.movie_id = movie_link.linked_movie_id) And (cast_info.person_id = name.id) And (person_info.info_type_id = info_type.id) And (info_type.info = 'mini biography') And (name.id = person_info.person_id) And (person_info.note IS NOT NULL)